# 企业级数据平台架构设计（PB 级，流批一体）

> 目标：构建一套支持 **PB 级数据规模**、同时支持 **实时与离线计算** 的企业级数据平台，覆盖数据湖、OLAP、搜索引擎、图数据库、实时处理、离线处理与数据同步。

---

## 1. 数据平台整体架构

### 1.1 总体架构图（逻辑）

```text
                ┌───────────────────────── 接入层 ──────────────────────────┐
                │ 业务系统 / 日志 / DB / API / 文件 / IoT / 第三方数据源      │
                └────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
                          ┌──────────────────────┐
                          │ 数据接入与同步层      │
                          │ CDC / Batch / MQ     │
                          │ Kafka / Connect / FDT│
                          └──────────────────────┘
                                      │
                    ┌─────────────────┴─────────────────┐
                    ▼                                   ▼
           ┌─────────────────────┐               ┌─────────────────────┐
           │ 实时处理引擎         │               │ 离线计算引擎         │
           │ Flink / Kafka Streams│              │ Spark / Flink Batch │
           └─────────────────────┘               └─────────────────────┘
                    │                                   │
                    └─────────────────┬─────────────────┘
                                      ▼
                     ┌────────────────────────────────────┐
                     │ 数据湖（Lakehouse）                │
                     │ Object Storage + Iceberg/Hudi      │
                     └────────────────────────────────────┘
                        │              │               │
                        ▼              ▼               ▼
             ┌────────────────┐ ┌────────────────┐ ┌────────────────┐
             │ OLAP 数仓       │ │ 搜索引擎        │ │ 图数据库         │
             │ ClickHouse/Trino│ │ OpenSearch/ES  │ │ JanusGraph/Neo4j│
             └────────────────┘ └────────────────┘ └────────────────┘
                        │              │               │
                        └─────── 查询与服务层（API/SQL/GraphQL） ───────┘
                                      │
                                      ▼
                           BI / 可视化 / 检索 / 图分析
```

### 1.2 设计要点

- **湖仓一体**：数据湖作为统一事实源，OLAP 提供高并发分析服务。
- **多引擎协同**：OLAP 负责聚合分析，搜索负责全文检索，图数据库负责关系推理。
- **流批一体**：实时计算保障分钟级可见，离线计算保障口径一致与历史回溯。
- **解耦同步**：以 Kafka 为核心数据总线，CDC 与批量同步并行。
- **PB 级扩展**：计算与存储分离，全部组件支持横向扩容。

---

## 2. 数据湖架构

### 2.1 技术选型

- **存储层**：S3 / OSS / HDFS（对象存储优先）
- **表格式**：Apache Iceberg（推荐）或 Hudi
- **文件格式**：Parquet（列式压缩）

### 2.2 湖内分区与组织

- 分区策略：`dt`（日期） + `tenant_id` + `domain`
- 大表按时间 + 主业务键二级分桶
- 小文件治理：Compaction（定时合并）

### 2.3 事务与版本

- ACID 事务写入（并发安全）
- Snapshot + Time Travel（支持历史版本回放）
- Schema Evolution（字段增删改可控）

### 2.4 PB 级能力保障

- 存储计算分离，存储无限扩展
- 按冷热分层（热/温/冷）降低成本
- 元数据服务高可用部署，避免目录扫描瓶颈

---

## 3. 数据仓库（OLAP）架构

### 3.1 架构模式

- **明细在湖、加速在仓**：
  - 湖中保留全量明细
  - 仓中保留高频聚合与宽表
- 查询入口：Trino（联邦查询）+ ClickHouse（高性能 OLAP）

### 3.2 OLAP 数据模型

- 星型/雪花模型 + 宽表模型并行
- 事实表：行为事实、交易事实、事件事实
- 维度表：时间、地域、实体、组织、设备

### 3.3 性能设计

- 分区裁剪 + 主键排序 + 稀疏索引
- 物化视图（高频指标预聚合）
- 查询缓存（结果集缓存）

### 3.4 负载目标（建议）

- TB~PB 规模可水平扩展
- 常规 OLAP 查询 P95 < 3s
- 并发查询能力可按节点线性提升

---

## 4. 实时流处理架构

### 4.1 实时链路

`Source -> Kafka -> Flink -> Iceberg/ClickHouse/OpenSearch/Graph`

### 4.2 核心组件

- Kafka：消息缓冲、削峰填谷、事件解耦
- Flink：实时 ETL、窗口聚合、维表 Join、规则计算
- Redis/HBase：实时维表或特征查询

### 4.3 实时处理能力

- 事件时间 + Watermark 处理乱序
- Checkpoint / Savepoint 保障故障恢复
- Exactly-once（端到端一致性）

### 4.4 场景输出

- 实时指标写入 ClickHouse
- 全文索引实时写入 OpenSearch
- 实体关系实时写入图数据库
- 标准明细持续落湖（Iceberg）

---

## 5. 离线计算架构

### 5.1 离线处理链路

`Lake(ODS) -> 清洗标准化(DWD) -> 主题汇总(DWS) -> 应用数据(ADS)`

### 5.2 计算引擎与编排

- 计算：Spark SQL / Flink Batch
- 编排：Airflow / DolphinScheduler / Argo Workflows
- 调度：支持依赖、重试、补数、回溯重算

### 5.3 关键离线任务

- 全量历史回算
- 主数据合并与实体消歧
- 图关系重建（复杂多跳）
- 训练特征集构建

### 5.4 离线一致性

- 统一指标口径（语义层）
- T+1 稳定产出，必要场景支持小时级批处理

---

## 6. 数据同步架构

### 6.1 同步类型

1. **CDC 实时同步**：MySQL/PG/Oracle -> Kafka（Debezium/Flink CDC）
2. **批量同步**：DB/文件/对象存储 -> Lake（Sqoop/SeaTunnel）
3. **服务订阅同步**：内部微服务事件 -> Kafka Topic

### 6.2 同步策略

- 增量优先，全量兜底
- 幂等写入（去重键 + 版本号）
- 延迟监控与积压告警

### 6.3 多目标分发

- 一份标准化数据可同时分发至：
  - 数据湖（归档与明细分析）
  - OLAP（聚合分析）
  - 搜索（全文检索）
  - 图数据库（关系分析）

### 6.4 一致性保障

- 事件 ID + 事务时间戳
- 重放机制（Kafka Retention + 回放任务）
- 对账任务（日级/小时级）

---

## 7. 数据分层设计

### 7.1 分层定义

- **ODS（原始层）**：原样落地，保留审计信息
- **DWD（明细标准层）**：清洗、字段标准化、统一编码
- **DWS（主题汇总层）**：按主题聚合（人、组织、设备、事件）
- **ADS（应用服务层）**：面向报表、检索、风控、图分析输出

### 7.2 分层规范

- ODS 不直接对外服务
- DWD 保证可追溯、可重算
- DWS 统一指标口径
- ADS 面向具体业务场景，避免重复建设

### 7.3 命名与生命周期建议

- 命名：`{layer}_{domain}_{entity}_{granularity}`
- 生命周期：ODS 长保留，DWD/DWS 按业务保留，ADS 可短周期迭代

---

## 推荐技术栈（企业可落地组合）

### A. 开源增强型（推荐）

- **数据湖**：S3/OSS + Iceberg + Parquet
- **实时处理**：Kafka + Flink
- **离线处理**：Spark + Airflow
- **OLAP**：ClickHouse + Trino
- **搜索引擎**：OpenSearch
- **图数据库**：JanusGraph（存储后端 Cassandra/Scylla）或 Neo4j
- **元数据与治理**：DataHub/OpenMetadata + Apache Ranger
- **监控**：Prometheus + Grafana + Loki

### B. 云原生托管型（快速交付）

- 托管对象存储 + 托管 Kafka + 托管 Flink
- 托管湖仓（Iceberg/Hudi）+ 托管 OLAP
- 托管搜索 + 托管图数据库

---

## 容量与高可用建议（PB 级）

- 存储：对象存储三副本或跨可用区冗余
- 计算：Flink/Spark/OLAP 全部无状态或弱状态节点横向扩容
- Kafka：多副本 + 跨 AZ 部署
- 元数据与调度：主从/RAFT 高可用
- 灾备：同城双活 + 异地备份（RPO < 15min, RTO < 1h）

---

## 总结

本方案通过“**Lakehouse + OLAP + Search + Graph + Streaming + Batch**”六大核心能力协同，构建可扩展、可治理、可审计的数据平台底座，满足 PB 级数据管理、实时分析与离线深度计算的企业级需求。
