# 案件串并查询：版本展示与人员卡片设计

## 1. 查询时是否展示“最新版本”？

结论：**默认展示最新有效版本 + 提供历史版本回溯能力**。

推荐策略：

1. **默认模式（LATEST）**
   - 面向业务用户默认只看“最新可信快照”，减少重复与噪声。
   - 适用于案件研判、日常检索、指挥看板。

2. **回溯模式（AS_OF_TIME / VERSION）**
   - 支持按“时间点”或“版本号”查看历史状态。
   - 适用于审计追责、复盘、证据链还原。

3. **变更模式（DIFF）**
   - 对比两次快照（例如 v12 vs v15），展示新增关系、失效关系、属性变化。

---

## 2. 为什么不建议只看最新？

如果只看最新会丢失：

- 关键关系的出现与消失时间（时态关系）
- 导入批次差异（哪一次导入引入了该关系）
- 误清洗/误归并后的纠错证据

所以企业级实践应是：**默认最新 + 随时可追溯历史**。

---

## 3. 是否要建立“人员卡片”？

结论：**必须建立人员卡片（Person Profile Card）**。

人员卡片是“查询展示层的统一读模型”，用于把多源、多版本、多案件数据聚合成一张可解释页面。

---

## 4. 人员卡片建议结构

## 4.1 基础身份区（Identity）

- person_id（平台统一ID）
- 主身份标识：id_no（身份证）
- 姓名、性别、出生日期
- 风险等级、可信度评分
- 最新更新时间

## 4.2 关联资产区（Assets）

- 银行卡列表（卡号脱敏、发卡行、状态）
- 手机号、邮箱、设备ID
- 地址、组织、账号

## 4.3 案件关联区（Case Linkage）

- 命中案件数量
- 案件列表（case_id、名称、状态、命中时间）
- 在每个案件中的角色（嫌疑人/联系人/受害人等）

## 4.4 关系图摘要区（Graph Summary）

- 1跳/2跳高频关系实体
- 关键路径（人 -> 卡 -> 交易 -> 案件）
- 关系强度与时间分布

## 4.5 时态与版本区（Timeline & Version）

- 首次出现时间、最近出现时间
- 版本时间轴（v1..vn）
- 关键变更事件（新增卡、解绑卡、关系失效）

## 4.6 审计区（Audit）

- 数据来源系统
- 导入批次号
- 清洗规则选择记录
- 操作审计日志

---

## 5. 查询接口建议

### 5.1 人员卡片查询

- `GET /api/v1/profile/person/{personId}?view=LATEST`
- `GET /api/v1/profile/person/{personId}?view=AS_OF_TIME&ts=2026-03-01T00:00:00Z`
- `GET /api/v1/profile/person/{personId}/versions`
- `GET /api/v1/profile/person/{personId}/diff?v1=12&v2=15`

### 5.2 串并查询

- `GET /api/v1/linkage/person?idNo=...`
  - 返回：同人命中案件列表、每案证据摘要、关键关系路径。

- `GET /api/v1/linkage/card?cardNo=...`
  - 返回：同卡命中案件列表、关联持卡人、交易桥接路径。

---

## 6. 存储与计算建议（落地）

1. **图数据库**
   - 存储实体关系与时态边，承担串并主查询。

2. **ES**
   - 承担人员卡片文本检索、条件筛选、快速召回。

3. **卡片读模型表（建议新增）**
   - `person_profile_snapshot`（最新快照）
   - `person_profile_version`（版本历史）

4. **更新机制**
   - 导入/清洗完成后触发“卡片重建事件”。
   - 增量更新优先，夜间全量校准。

---

## 7. 展示策略建议（给前端）

默认展示：

- 卡片头部显示“最新快照时间”
- 案件串并结果按“近30天活跃度 + 风险分”排序
- 一键切换“历史版本视图”

避免误判：

- 显示“同名非同人”的置信度提示
- 对身份证缺失/异常数据单独打标

---

## 8. 最终建议

- **要展示最新版本吗？** 要，默认展示最新。
- **要不要建人员卡片？** 要，且应做成版本化卡片。
- **最佳实践**：`最新快照查询 + 历史回溯 + 差异对比 + 图关系解释` 四件套同时具备。

